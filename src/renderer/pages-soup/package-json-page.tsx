// import React from "react";
import styled from 'styled-components'

interface props {
  style: any
}

export const PackageJsonPage: React.FC<props> = ({ style }) => {
  return <Container style={style}>POOOP</Container>
}

const Container = styled.div``
